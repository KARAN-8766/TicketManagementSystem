
@model TicketHandler.core.Login

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Ticket Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/login.css" />
</head>
<body>

    <div class="login-container">
        <!-- LEFT SIDE (LOGO) -->
        <div class="login-left d-flex flex-column justify-content-center align-items-center text-white">
            <img src="~/css/logo6.png" alt="TMS Logo" class="logo mb-3" />
            <h2 class="fw-bold text-center">Ticket Management System</h2>
            <p class="text-center opacity-75 mt-2">Manage and track tickets efficiently</p>
        </div>

        <!-- RIGHT SIDE (FORM) -->
        <div class="login-right d-flex justify-content-center align-items-center">
            <div class="card login-card shadow-lg p-4 rounded-4">
                <h3 class="text-center mb-4">Welcome Back</h3>
                @if (ViewBag.Message != null)
                {
                    <div class="form-control d-flex justify-content-center" style="color:red;">@ViewBag.Message</div>
                }
                <form asp-action="Login" method="post">
                    <div class="mb-3">
                        <label for="Email" class="form-label">Email</label>
                        <input type="email" id="Email" name="Email" class="form-control" asp-for="Email">
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label for="Password" class="form-label">Password</label>
                        <input type="password" id="Password" name="Password" class="form-control form-control-lg" asp-for="Password">
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <button type="submit" class="btn btn-dark w-100 py-2 mt-2 rounded-3">Login</button>
                </form>
            </div>
        </div>
    </div>

</body>
</html>



@* <h2 class="d-flex justify-content-center">Please Sign-In</h2>

<form method="post">
    @if (ViewBag.Message != null)
    {
        <div class="form-control d-flex justify-content-center" style="color:red;"><h4>@ViewBag.Message</h4></div>
    }
    <br />
    <div class="container">
        <div class="row mb-3">
            <label class="col-sm-4 col-form-label d-flex justify-content-center align-content-center" for="email">Email</label>
            <div class="col-sm-6">
                <input type="text" class="form-control" asp-for="Email" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
        </div>
        <div class="row mb-3">
            <label class="col-sm-4 col-form-label d-flex justify-content-center align-content-center" for="password">Password</label>
            <div class="col-sm-6">
                <input type="password" class="form-control " asp-for="Password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-sm-4"></div>
            <div class="col-sm-6">
                <button type="submit" class="btn btn-primary w-100">Login</button>
            </div>
        </div>
    </div>
</form> *@